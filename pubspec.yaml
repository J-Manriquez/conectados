name: conectados 
description: "A new Flutter project." 
publish_to: 'none' 
version: 0.1.0 

environment: 
  sdk: '>=3.8.0-278.1.beta <4.0.0' 

dependencies: 
  flutter: 
    sdk: flutter 
  permission_handler: ^12.0.0+1 
  flutter_blue_plus: ^1.5.2 
  flutter_local_notifications: ^16.0.0  # Especifica una versión concreta
  notification_listener_service: ^0.3.4 
  shared_preferences: ^2.0.15 
  flutter_bluetooth_serial: ^0.4.0 
  flutter_background_service: ^5.1.0 
  device_apps:
    path: .pub-cache-overrides/device_apps

dev_dependencies: 
  flutter_test: 
    sdk: flutter 
  flutter_lints: ^5.0.0 

flutter: 
  uses-material-design: true
